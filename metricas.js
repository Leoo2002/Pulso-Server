// Archivo adaptado: la implementación real de métricas fue movida a
// `src/services/metricas.service.js`. Para mantener compatibilidad con
// scripts que puedan requerir este archivo desde la raíz, se reexporta
// el servicio desde la carpeta `src`.

module.exports = require('./src/services/metricas.service');